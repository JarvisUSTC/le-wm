defaults:
  - lewm
  - _self_

output_model_name: lewm_ds
subdir: ds_${hydra:job.id}

loss:
  sigreg:
    weight: 0.09
    kwargs:
      knots: 17
      num_proj: 1024

  decision:
    action_weight: 0.1
    reward_weight: 0.1
    value_weight: 0.05
    gamma: 0.99
